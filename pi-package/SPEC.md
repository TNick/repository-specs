# Pi Package

Use this profile for a package installed by Pi and discovered through its
extension, skill, prompt, or theme resources.

## Required

- Declare every auto-loaded resource in the package manifest.
- Keep reference examples separate from installed resources.
- Document install, local development, reload, and validation commands.
- Keep extensions small and delegate reusable behavior to tested modules.
- Validate the packaged artifact before publishing it.
